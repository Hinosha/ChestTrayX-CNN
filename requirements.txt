# Core Dependencies
streamlit>=1.28.0
tensorflow>=2.16.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0



# Alternative lighter versions for deployment (if facing memory issues)
# tensorflow-cpu>=2.16.0  # Use instead of tensorflow for CPU-only deployment
# opencv-python-headless>=4.8.0  # Use instead of opencv-python for headless servers